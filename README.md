# CloudDrive - Облачное хранилище файлов

Современное веб-приложение для управления файлами в облаке. Позволяет пользователям загружать, хранить, организовывать и управлять своими файлами через удобный веб-интерфейс.

![Version](https://img.shields.io/badge/version-0.1.0-blue.svg)
![Next.js](https://img.shields.io/badge/Next.js-16.1.6-black)
![React](https://img.shields.io/badge/React-19.2.0-blue)
![TypeScript](https://img.shields.io/badge/TypeScript-5.0-blue)

## 🚀 Основные возможности

- ✅ **Загрузка файлов** - Множественная загрузка файлов с drag & drop
- ✅ **Управление файлами** - Просмотр, скачивание, удаление файлов
- ✅ **Организация** - Фильтрация по типам файлов (фото, видео, аудио, документы, архивы)
- ✅ **Избранное** - Добавление файлов в избранное для быстрого доступа
- ✅ **Корзина** - Мягкое удаление с возможностью восстановления
- ✅ **Поиск и сортировка** - Поиск файлов и сортировка по различным параметрам
- ✅ **Множественное выделение** - Выделение нескольких файлов одновременно
- ✅ **Статистика** - Просмотр статистики использования хранилища
- ✅ **Профиль пользователя** - Управление профилем и настройками
- ✅ **Аутентификация** - Безопасная регистрация и вход в систему

## 🛠 Технологический стек

### Frontend
- **Next.js 16.1.6** - React фреймворк с SSR
- **React 19.2.0** - UI библиотека
- **TypeScript 5.0** - Типизированный JavaScript
- **Ant Design 5.28.1** - UI компоненты
- **Zustand 5.0.11** - Управление состоянием
- **Axios 1.13.2** - HTTP клиент
- **SCSS** - Стилизация
- **react-hot-toast** - Уведомления
- **react-selecto** - Множественное выделение

### Инструменты разработки
- **ESLint** - Линтинг кода
- **TypeScript** - Проверка типов

## 📋 Требования

- Node.js 18+ 
- npm или yarn
- Backend сервер (см. [Backend репозиторий](../cloud-storage-backend))

## 🔧 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd cloud-storage-frontend
```

### 2. Установка зависимостей

```bash
npm install
# или
yarn install
```

### 3. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```env
NEXT_PUBLIC_API_URL=http://localhost:3001
```

> **Примечание:** Убедитесь, что URL соответствует адресу вашего backend сервера.

### 4. Запуск в режиме разработки

```bash
npm run dev
# или
yarn dev
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

### 5. Сборка для production

```bash
npm run build
npm run start
# или
yarn build
yarn start
```

## 📁 Структура проекта

```
cloud-storage-frontend/
├── public/                 # Статические файлы
├── src/
│   ├── api/               # API клиент и методы
│   │   ├── dto/           # Data Transfer Objects
│   │   ├── auth.ts        # API аутентификации
│   │   └── files.ts       # API работы с файлами
│   ├── components/        # React компоненты
│   │   ├── auth/          # Компоненты аутентификации
│   │   ├── FileCard/      # Карточка файла
│   │   ├── FileList/      # Список файлов
│   │   ├── header/        # Шапка приложения
│   │   └── ...
│   ├── core/              # Основные настройки
│   │   └── axios.ts       # Конфигурация HTTP клиента
│   ├── hooks/             # Кастомные React хуки
│   │   ├── useFileActions.ts
│   │   ├── useFilesInitialization.ts
│   │   └── useToggleFavorite.ts
│   ├── layouts/           # Макеты страниц
│   ├── modules/           # Бизнес-модули
│   │   └── Files.tsx      # Модуль работы с файлами
│   ├── pages/             # Страницы Next.js
│   │   ├── dashboard/     # Страницы дашборда
│   │   └── ...
│   ├── store/             # Zustand stores
│   │   ├── filesStore.ts  # Store для файлов
│   │   └── uiStore.ts    # Store для UI состояния
│   ├── styles/            # Глобальные стили
│   └── utils/             # Утилиты
├── .env.example           # Пример переменных окружения
├── next.config.ts         # Конфигурация Next.js
├── tsconfig.json          # Конфигурация TypeScript
└── package.json           # Зависимости проекта
```

## 🎯 Основные функции

### Управление файлами
- Загрузка файлов (одиночная и множественная)
- Просмотр файлов в grid/list режимах
- Скачивание файлов
- Удаление файлов (в корзину или навсегда)
- Восстановление из корзины
- Добавление в избранное

### Фильтрация и поиск
- Фильтрация по типам: фото, видео, аудио, документы, архивы
- Поиск по имени файла
- Сортировка по имени, дате, размеру, типу

### Множественное выделение
- Выделение файлов через drag & select
- Выделение через Ctrl/Cmd + Click
- Выделение диапазона через Shift + Click
- Массовые операции с выделенными файлами

### Статистика
- Общее количество файлов
- Использованное место
- Статистика по категориям
- Топ категории файлов

## 🔐 Аутентификация

Приложение использует JWT токены для аутентификации. Токен хранится в cookies и автоматически добавляется к каждому запросу.

### Страницы аутентификации
- `/dashboard/auth` - Страница входа и регистрации

## 📝 Скрипты

```bash
# Запуск в режиме разработки
npm run dev

# Сборка для production
npm run build

# Запуск production сборки
npm run start

# Проверка кода линтером
npm run lint
```

## 🌐 API интеграция

Приложение взаимодействует с backend API через Axios. Все запросы автоматически включают JWT токен из cookies.

### Основные endpoints:
- `POST /auth/login` - Вход в систему
- `POST /auth/register` - Регистрация
- `GET /files` - Получение списка файлов
- `POST /files/upload` - Загрузка файла
- `DELETE /files` - Удаление файлов
- `PATCH /files/:id/favorite` - Переключение избранного
- `PATCH /files/restore` - Восстановление файлов

## 🎨 Стилизация

Проект использует SCSS модули для стилизации компонентов. Глобальные стили и CSS переменные находятся в `src/styles/globals.css`.

### CSS переменные:
```css
--primary-color: #1a73e8
--text-primary: #202124
--bg-primary: #ffffff
--border-color: #dadce0
--shadow-sm/md/lg: различные тени
--radius-sm/md/lg/xl: радиусы скругления
```

## 🧩 Управление состоянием

Проект использует **Zustand** для управления состоянием:

- `filesStore` - Состояние файлов (список, загрузка, ошибки)
- `uiStore` - UI состояние (выделение, режим просмотра, фильтры)

## 🔧 Кастомные хуки

Проект включает переиспользуемые хуки:

- `useFilesInitialization` - Инициализация файлов в store
- `useFileActions` - Действия с файлами (удаление, восстановление, скачивание)
- `useToggleFavorite` - Переключение статуса избранного

## 📦 Зависимости

### Основные:
- `next` - React фреймворк
- `react` & `react-dom` - UI библиотека
- `antd` - UI компоненты
- `zustand` - State management
- `axios` - HTTP клиент
- `react-hot-toast` - Уведомления

### Полный список зависимостей см. в `package.json`

## 🐛 Отладка

### Логирование
Проект использует кастомный logger (`src/utils/logger.ts`), который:
- Логирует только в development режиме (кроме ошибок)
- Ошибки всегда логируются

### Обработка ошибок
- Глобальная обработка ошибок через Axios interceptors
- Автоматический редирект на страницу входа при 401 ошибке
- Понятные сообщения об ошибках для пользователя

## 🚀 Деплой

### Vercel (рекомендуется)
1. Подключите репозиторий к Vercel
2. Установите переменные окружения:
   - `NEXT_PUBLIC_API_URL` - URL вашего backend API
3. Деплой произойдет автоматически

### Другие платформы
Приложение можно деплоить на любую платформу, поддерживающую Next.js:
- Netlify
- AWS Amplify
- DigitalOcean App Platform
- И другие

## 📚 Дополнительная документация

- [CHANGELOG.md](./CHANGELOG.md) - История изменений
- [STRUCTURE.md](./STRUCTURE.md) - Подробная структура проекта

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект является частью дипломной работы.

## 👨‍💻 Автор

Разработано как дипломный проект.

## ⚠️ Важные замечания

1. **Backend обязателен** - Приложение требует работающий backend сервер
2. **Переменные окружения** - Не забудьте создать `.env.local` файл
3. **CORS** - Убедитесь, что backend настроен для работы с вашим frontend доменом
4. **Токены** - JWT токены хранятся в cookies, убедитесь в правильной настройке безопасности

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте, что backend сервер запущен и доступен
2. Убедитесь, что переменные окружения настроены правильно
3. Проверьте консоль браузера на наличие ошибок
4. Проверьте логи сервера

---

**Приятного использования! 🎉**
